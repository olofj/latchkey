# Test harness artifacts (regenerate with gen-certs.sh)
testing/harness/*.pem
testing/harness/*.key
testing/harness/*.der
testing/harness/*.srl
testing/harness/*.log
*.ndjson

# The aperture-plus fork is its own git repository (branch latchkey, remote
# `upstream`), not a submodule of this one. See docs/DECISIONS.md; recreate it
# with scripts/bootstrap.sh.
app/

# Xcode / build
app/build/
app/.dd/
DerivedData/
*.xcresult
.DS_Store
